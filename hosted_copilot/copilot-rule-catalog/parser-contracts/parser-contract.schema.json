{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "schemaVersion", "parserId", "assessmentCardinality", "behaviorFiles"],
  "properties": {
    "$schema": {
      "const": "parser-contract.schema.json"
    },
    "schemaVersion": {
      "const": 1
    },
    "parserId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*-v[1-9][0-9]*$"
    },
    "assessmentCardinality": {
      "enum": ["zero-to-many", "exactly-one"]
    },
    "behaviorFiles": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "^[A-Za-z0-9._/-]+$"
      }
    }
  }
}
